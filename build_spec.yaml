version: 0.1
component: build

steps:
  - type: Command
    name: Run Regression Test
    command: |
      mvn clean test
outputArtifacts:
  - name: loginform-jar
    type: BINARY
    location: target/*.jar
